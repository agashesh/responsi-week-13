{
  "name": "cv-node-express-react-render",
  "version": "1.0.0",
  "description": "Website CV React dengan Backend Node.js Express JSON siap deploy ke Render",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend && node build.js",
    "start": "npm start --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "keywords": [
    "nodejs",
    "express",
    "react",
    "vite",
    "render",
    "cv"
  ],
  "author": "BCI Class - AMIKOM University",
  "license": "ISC",
  "dependencies": {
    "all": "^0.0.0"
  }
}
